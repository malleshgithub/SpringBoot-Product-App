spring:
  datasource:
    username: Root@1992
    password: Root@1992
    url: jdbc:mysql://localhost:3306/mydb?useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
server:
  port: 8081      
   